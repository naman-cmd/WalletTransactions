import { WalletTransactionDto } from './wallet-transaction-dto';

describe('WalletTransactionDto', () => {
  it('should create an instance', () => {
    expect(new WalletTransactionDto()).toBeTruthy();
  });
});
